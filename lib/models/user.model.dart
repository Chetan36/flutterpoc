class UserModel {
  int id;
  String firstName;
  String lastName;
  String contactNumber;
  String emailId;
  String password;

  UserModel({this.id, this.firstName, this.lastName, this.contactNumber, this.emailId, this.password});
}
